# 真实模型问题修复与回归（2026-09-18）

首轮发现的四项问题已修复。本轮使用原有模拟数据，在真实 Chrome、DeepSeek、DSH/MCP 和宿主核验链路上复测六个问题，六个符合预期。这是针对性回归，不是总体准确率或上线验收结论；首次计算仍偏慢。

## 修复内容

1. 用户明确指定的版本、区域等范围由宿主解析并与最终选择核对。没有北京与 R1 的对应依据时请求澄清，不把真实城市猜成模拟区域；追问保留明确指定的区域。结果卡片展示实际区域或整体样本。范围不符的数字不会发布，会话仍可补充条件。
2. 明确比较工具的差异字段与原始比例证据的区别。引用选择不合法时最多允许一次只纠正回答的模型请求，沿用当前轮预算、总时限和证据；禁止再调用工具，纠正后仍完整核验。
3. 上下文参数第一次出错时允许一次纠正；成功取得本轮上下文前不能执行业务工具。最终校验与这项规则一致。
4. 补充笑话、段子等明确无关表达的本地拒绝规则，保留范围内自由表达入口。

范围解析目前依赖明确编号、已登记别名和有限语言模式，是保守约束，不是完整自然语言理解器。未知条件可能需要澄清；本次检查也不能证明任意无关表达都能零 token 拦截。

## 真实浏览器回归

| 问题 | 结果 | 新增模型请求 | 耗时 |
|---|---|---:|---:|
| 原北京地区问题，要求不能用全部玩家代替 | 本地澄清，没有发布 R1 数字 | 0 | 0.115 秒 |
| 给我讲个笑话吧 | 本地拒绝 | 0 | 0.059 秒 |
| 比较第五/六次更新，并询问是否能认定不喜欢新剧情 | 比例与百分点差核验通过，指出因果限制 | 4 | 125.989 秒 |
| 第五次更新，地图探索到支线体验的关联指标 | 返回正确规则与三个关联指标 | 4 | 29.522 秒 |
| 第六次更新，只看 R1，给分子分母比例 | 614 / 1606 = 38.23% | 3 | 10.487 秒 |
| 那第五次更新呢，沿用刚才的区域和口径 | 仍为 R1，555 / 1481 = 37.47% | 3 | 10.498 秒 |

比较结果为 37.91149487% → 39.27327028%，增加 1.36177541 个百分点。关联结果对应 A02 → A01、validation_V5：N=4600、nA=1235、nB=1238、nAB=515；支持度约 11.20%、置信度约 41.70%、提升度约 1.55。审核脚本逐项对照实际证据、冻结资产与计算值，也检查了最终选择的版本和区域，没有只按接口成功判定通过。

证据位于本地 `state/repair-live-20260918/`：`results.json`、`audit.json`、逐案 JSON 和真实截图。原始错误回答重新经过新增范围校验也被拒绝，记录在 `old-answer-guard.json`。该目录被 Git 忽略，不应分享其中的浏览器 Cookie 状态文件。首轮失败基线保留在 [原验收报告](LIVE_VALIDATION_2026-09-18.md)，没有改写历史结果。

真实回归的比较回答直接生成了正确引用，因此另用离线模型桩故意产生错误引用、错误首轮上下文，通过真实 DSH/MCP 验证两个纠正分支。复现命令：

```powershell
.\.venv-core\Scripts\python.exe -B scripts/verify_live_repairs.py --integration
```

其中引用纠正保持同一 turn、总计五次模型桩请求，最后一次 `tool_choice=none`。脚本同时检查范围替换、追问、否定条件、上下文次序及范围不符后的澄清状态。该命令不调用付费 API。

## Token 与费用

- 本轮新增 14 次真实请求，全部结算；输入 274843 token（缓存命中 208000、未命中 66843），输出 3152 token，共 277995 token。
- 按程序保守单价估算，本轮新增约 **0.167222 元**；这不是供应商账单。单价依据与峰谷差异见首轮报告。
- 沿用 `live-check-20260918-network` 原预算周期，没有通过新建账本重置额度。该周期累计 53 次请求，保守费用估算 0.55248144 元，当前未知费用为 0。
- 更早网络失败周期 `live-check-20260918` 的 0.090192 元未知预留保持原状，未删除、未假定免费。

真实回归源版本为 `product1580752d81b0c48029ddf2bff7`。之后补充了纠正共用原时限、纠正最多一个 HTTP 请求、范围校验失败转澄清的保护及本报告；这些新增保护通过离线回归验证。冻结资产仍为 60 项、`asset143f285f40e8357711e76382ff`。没有提交或推送 Git。

## 下一阶段

先优化首次计算与请求上下文开销，再扩充不同说法、连续追问和异常路径回归。此次首次比较仍需约 126 秒，后续单项查询约 10 秒；不同问题及缓存状态不能直接用于计算性能提升率。默认演示恢复为离线，真实模型启动仍须明确使用 live 模式。暂不进入 Vercel、服务器、Docker 或 MySQL 部署阶段。
