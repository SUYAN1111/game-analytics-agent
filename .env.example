# Copy to .env and fill in your key locally. Never commit the real .env.
# scripts/start-web.ps1 -Mode live reads only DEEPSEEK_API_KEY from this file.
# An existing process environment key takes precedence. Offline mode ignores it.
DEEPSEEK_API_KEY=
