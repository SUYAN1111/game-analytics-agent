# Documentation only; no automatic dotenv loading.
# APP_ASSET_DIR=<extracted assets directory>
# APP_STATE_DIR=<independent writable directory>
# APP_DEBUG=0
# DEEPSEEK_API_KEY must be set interactively in process environment for --live.
